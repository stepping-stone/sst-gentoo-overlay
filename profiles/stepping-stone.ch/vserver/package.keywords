app-backup/onlinebackup amd64 x86

# force stable keyword on zabbix-1.8 since Gentoo has now zabbix-2 stable
# and zabbix-1.8 lost it's stable keyword in the process
=net-analyzer/zabbix-1.8* amd64 x86

dev-ruby/facter amd64 x86
app-admin/puppet amd64 x86
dev-ruby/hiera amd64 x86
app-vim/puppet-syntax amd64 x86
